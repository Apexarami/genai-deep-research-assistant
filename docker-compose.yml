services:
  research-api:
    build: .
    container_name: genai-deep-research-assistant
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=GenAI Deep Research Assistant
      - KNOWLEDGE_BASE_DIR=data/knowledge_base
      - DEFAULT_TOP_K=4
      - LLM_PROVIDER=local_demo
