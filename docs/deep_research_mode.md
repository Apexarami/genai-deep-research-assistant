# Deep Research Mode

Deep Research Mode does more than simple PDF question answering.

It creates a research plan, expands the user question into several search queries, retrieves broader evidence from the uploaded PDF, estimates confidence, and then creates one final answer with page references.

If Ollama is available, it uses a local model to generate a smoother answer. If Ollama is not available, it still works in extractive mode.
